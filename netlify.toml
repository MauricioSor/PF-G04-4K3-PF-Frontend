# Netlify build config
[build]
  command     = "npm run build"
  publish     = "dist"

# SPA fallback: todas las rutas desconocidas → index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
